# Number of CPU cores to use 
[cpu_cores]
cores = 2

# Database settings (Mongo DB)
[db_settings]
db = malware 
collection = samples 

# Data locations (use full paths!)
[data_location]
yara_rules = /home/solvent/lob_scripts/yara_rules/
log_location = /home/solvent/lob_scripts/zookeeper/log/
repository_dir = /home/solvent/lob_scripts/zookeeper/storage/
